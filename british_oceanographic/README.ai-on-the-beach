In bash

  ncdump <input>.nc

The bathysphere.py script reads it

However, this input was discarded since Basemap's continent boundaries,
which agree nicely with Google map lat/lon coordinates, do NOT agree
with the bathysphere data. Errors in latitude up to 0.5 degree were seen